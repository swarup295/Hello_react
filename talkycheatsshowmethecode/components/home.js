import SearchBar from "./searchBar";
import RestaurantList from "./restaurantList";

const Home = () => {
  return (
    <div>
      <SearchBar />
      <RestaurantList />
    </div>
  );
};

export default Home;
